import "./styles.css"
import classes from "./styles.module.css"

// Global styles are inlined via GM_addStyle in the production build.
// CSS module class names are hashed for scoping.
console.log("Title class:", classes.title)
